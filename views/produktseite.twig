{% extends 'layout.twig' %}

{% block main %}
    <div class="product-inner nr1">
        <div class="productdetail-images">
            <input type="radio" name="AorB" id="A" checked="checked">
            <input type="radio" name="AorB" id="B">
            <label for="A"></label>
            <label for="B"></label>
            <img src="{{ chocolate.imageA }}" class="A">
            <img src="{{ chocolate.imageB }}" class="B">
        </div>
        <div class="productdetail-details">
            <h2>Inhalte:</h2>
            <p>{{ chocolate.info }}</p>
            <div class="productdetail-ingredients">
                <div class="category-icon {{ chocolate.shape.className|default('shape-0') }}"></div>
                <div class="category-icon {{ chocolate.variety.className|default('variety-0') }}"></div>
                <div class="category-icon {{ chocolate.creme.className|default('cream-0') }}"></div>
                <div class="category-icon {{ chocolate.filling.className|default('stuffing-0') }}"></div>
                <div class="category-icon {{ chocolate.topping.className|default('topping-0') }}"></div>
            </div>
            <h2>Preis:</h2>
            <h3>{{ chocolate.price }} €</h3>
            <form method="post" action="/shopping-cart">
                <input type="hidden" name="chocolateId" value="{{ chocolate._id }}">
                <button>In den Warenkorb legen</button>
            </form>
        </div>
    </div>
{% endblock %}